# This is the configuration used to check the rubocop source code.
AllCops:
  Include:
    - Gemfile
    - Rakefile
    - bin/*
    - mp4_renamer.gemspec
    - lib/**/*.rb
    - test/**/*.rb
    - spec/**/*.rb
